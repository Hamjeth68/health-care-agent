# TODO

- [ ] Implement Terraform AWS provider credential support via `aws_profile` (optional) and optional `assume_role_arn`.
- [ ] Update `variables.tf` and `main.tf` accordingly.
- [ ] Run `terraform init` and `terraform plan` to confirm credential error is resolved.

