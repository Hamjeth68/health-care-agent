# Lean production runtime for the FastAPI service.
# torch/transformers/sentence-transformers stay in requirements.txt (dev/full);
# they are lazy-loaded on first /ask call and must be installed separately on
# instances large enough to hold them (t3.small or larger recommended).
fastapi==0.135.1
uvicorn==0.41.0
supabase>=2.3.4
python-dotenv>=1.0.0
requests==2.32.5

# RAG retrieval — small packages required for the /ask endpoint.
faiss-cpu==1.13.2
rank_bm25==0.2.2
numpy==2.4.2
